syntax = "proto3";
package data_center;
option go_package = "../data_center";


message MqttMessage {
  string mission = 1;             // 任務
  uint64 memberId = 2;                 // 用户ID
  string ticketId = 3;           // tickey ID
  string message = 4;
  string handicapSide = 5;       // 让球方
  string handicapValues = 6;     // 让球数
  string overUnderValues = 7;    // 大小球
  int64 startTime = 8;           // 开始时间
  int64 endTime = 9;             // 结束时间
}
